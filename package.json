{
    "name": "e2j-api",
    "version": "0.1.0",
    "scripts": {
        "devel": "export URL=http://127.0.0.1:1111 && netlify-lambda serve script/api/endpoint",
        "build": "netlify-lambda build script/api/endpoint"
    },
    "dependencies": {
        "axios": "^0.19.2"
    },
    "devDependencies": {
        "netlify-lambda": "^1.6.3"
    }
}
